## Resumo

(Uma linha descrevendo o que este PR faz.)

## Detalhes

- O que mudou:
- Por que mudou:
- Impacto em outras partes do sistema:

## Tipo de mudança

- [ ] `feat` (nova funcionalidade)
- [ ] `fix` (correção de defeito)
- [ ] `docs` (documentação)
- [ ] `refactor` (refatoração sem mudança de comportamento)
- [ ] `test` (testes)
- [ ] `chore` (configuração, infraestrutura)

## Roteiro de testes

(Comandos que o revisor deve executar para validar a mudança.)

```bash
# Exemplo:
python3 -m pytest
python3 run.py listar
```

## Checklist

- [ ] Os commits seguem o padrão Conventional Commits descrito em `CONTRIBUTING.md`.
- [ ] A suíte de testes passa localmente (`python3 -m pytest`).
- [ ] Testes foram adicionados ou ajustados para cobrir a mudança.
- [ ] A documentação foi atualizada quando aplicável (`README.md`, `RELATORIO_TESTES.md`, docstrings).
- [ ] O `.gitignore` foi respeitado: nenhum dado de acervo ou cache foi versionado por engano.

## Issues relacionadas

(Mencione números ou links de issues que este PR resolve, fecha ou referencia. Use `Closes #N` para fechar automaticamente após o merge.)
